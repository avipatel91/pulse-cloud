name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - name: Lint code
      uses: stefanoeb/eslint-action@1.0.0
      with:
        args: "index.ts src/**.ts"
